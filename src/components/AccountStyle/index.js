import classNames from 'classnames/bind';
import style from './AccountStyle.scss';

const cx = classNames.bind(style);

function AccountStyle({ children }) {
    return children;
}

export default AccountStyle;